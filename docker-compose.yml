services:
  portfolio-server:
    build: .
    container_name: portfolio-server
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data          # SQLite DB
      - ./inbox:/app/inbox        # Drop files here for manual registration
      - ./errors:/app/errors      # Download error logs dumped here
      - ./logs:/app/logs          # App logs
      - ./scripts:/app/scripts    # Utility scripts (reset_db, scan_bhavcopy_files, etc.)
    environment:
      - DB_PATH=/app/data/portfolio_server.db
      - ERRORS_PATH=/app/errors
      - LOGS_PATH=/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c",
             "import urllib.request; urllib.request.urlopen('http://localhost:8000/health').read()"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 30s
